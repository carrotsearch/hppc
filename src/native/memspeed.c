#include <stdio.h>#include <stdlib.h>int main(int argc, char ** argv){    if (argc != 1 + 3)    {        printf("Usage: repeats block-MB seekincr", argc);        exit(0);    }        int repeats = atoi(argv[1]);    int mem_size = atoi(argv[2]) * (1024 * 1024);    char * mem = (char *) malloc(mem_size);    int increment = atoi(argv[3]);    int i, j, index;    for (i = 0; i < repeats; i++)    {        for (j = 0, index = 0; j < mem_size; j++)        {            mem[index] = 2;            index = index + increment;            if (index < 0) index += mem_size;            else if (index > mem_size) index -= mem_size;        }    }    printf("%i", (int) mem[0]);    free(mem);}