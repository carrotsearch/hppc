Maven shortcuts
---------------

# All unit tests
mvn clean verify

# Package JAR files, no tests.
mvn clean package -Pquick


Eclipse workflow/ import
------------------------

# Install template processor
mvn install -am -pl :hppc-template-processor
# Copy over initial IDE .settings
mvn -Peclipse antrun:run
# Ready to import into Eclipse (using m2e).
